<?php
namespace html;


class _inputNumber extends _inputText{
	public function __construct($name, $value, $tooltip = 'none', $css = 'editing-input-number'){
		parent::__construct($name,$value, 12, 4, $tooltip,$css);
	}
}

function getNumberInput($inputName, $inputValue, $tooltip = 'none',$css = 'editing-input-number'){
	$e = new _inputNumber($inputName, $inputValue,$tooltip,$css);

	return $e->print();
}

function inputFieldNumber(
	$entity,
	$value,
	$id = 'numberId',
	$caption = '',
	$tooltip = NULL, 
	$disabled = 'false',
	$css = 'editing-input-number'){
		
	$input = getNumberInput($id, $value,$tooltip,$css);
	$element = captionedInput($caption,$input);
	return $element;	
		
	

}


?>
